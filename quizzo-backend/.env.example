# Server Configuration
PORT=5000
HOST=0.0.0.0  # 0.0.0.0 means listen on all network interfaces

# Client URL for CORS (use your frontend URL in production)
# Use * to allow any origin or specify exact URLs
CLIENT_URL=*

# Frontend URL for displaying connection information
FRONTEND_URL=http://localhost:5173

# Add other configurations as needed
# For example, if you integrate with a database later:
# DB_URI=mongodb://localhost:27017/quizzo 